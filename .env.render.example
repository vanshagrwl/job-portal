# Set these in the Render dashboard: Service → Environment
# (Render injects PORT automatically — do not set PORT unless you know you need to.)

MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster.mongodb.net/jobportal?retryWrites=true&w=majority
JWT_SECRET=your_jwt_secret_key_here_change_in_production
